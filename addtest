#!/bin/bash 

DB="tests.db"

echo "Enter INPUT (Ctrl+D to finish):"
input=$(cat)

echo "Enter EXPECTED OUTPUT (Ctrl+D to finish):"
output=$(cat)

{
  echo "###"
  echo "$input"
  echo "@@@"
  echo "$output"
} >> "$DB"

echo -e "\n✅ Test added to $DB"
